<ul class="data-table" data-test-account-list>
    <li class="table-row table-header">
      <div class="table-column">Name</div>
      <div class="table-column">Interest Rate</div>
      <div class="table-column">Balance</div>
      <div class="table-column">Monthly Payment</div>
    </li>
    {{#each @model as |account|}}
      <AccountList::Row @account={{account}} />
    {{/each}}
  </ul>